apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-products
  labels:
    app: service-products
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-products
  template:
    metadata:
      labels:
        app: service-products
    spec:
      containers:
        - name: service-products
          image: service-products:1.0
          imagePullPolicy: Never
          resources:
            limits:
              memory: 32Mi
              cpu: "50m"
          requests:
            memory: 16Mi
            cpu: "10m"
          ports:
            - containerPort: 50051
          envFrom:
            - secretRef:
                name: service-products-secret
